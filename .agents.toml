# Additional workspace directories for codex2 and agent2.
# Local editable dependencies declared in .dev/pyproject.toml.
# Paths are relative to this file's directory.
add_dirs = [
    '../commons',
    '../commons-1c',
    '../../Others/v8unpack',
    '../parse-1c-build',
]
