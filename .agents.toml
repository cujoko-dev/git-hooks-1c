# Additional workspace directories.
# Local editable dependencies declared in .dev/pyproject.toml.
# Paths may be absolute or relative to this file's directory.
add_dirs = [
    '../../others/v8unpack',
    '../../python/commons',
    '../../python/commons-1c',
    '../../python/parse-1c-build',
]
